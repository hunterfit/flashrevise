{
  "name": "flashrevise",
  "version": "1.0.0",
  "description": "FlashRevise — Application de révision par flashcards pour les élèves français",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Aucun build requis — app statique'",
    "cap:sync": "npx cap sync",
    "cap:android": "npx cap open android",
    "cap:ios": "npx cap open ios",
    "cap:run:android": "npx cap run android",
    "prepare:android": "npx cap add android && npx cap sync android",
    "prepare:ios": "npx cap add ios && npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "keywords": ["flashcards", "révision", "education", "maths", "langues"],
  "author": "FlashRevise",
  "license": "MIT"
}
